name: Linting with Black

on: push

jobs:
  lint-python-3-14:
    name: Check Black on Python 3.14 on Ubuntu
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.14"

      - name: Install package
        run: python -m pip install -e .[test]

      - name: Install black
        run: python -m pip install black

      - name: Lint package
        run: python -m black --check .on: push

jobs:
  lint-python-3-10:
    name: Check Black on Python 3.10 on Ubuntu
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install package
        run: python -m pip install -e ."[test]"

      - name: Install black
        run: python -m pip install black

      - name: Lint package
        run: python -m black --check .

  lint-python-3-14:
    name: Check Black on Python 3.14 on Ubuntu
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.14"

      - name: Install package
        run: python -m pip install -e ."[test]"

      - name: Install black
        run: python -m pip install black

      - name: Lint package
        run: python -m black --check .
